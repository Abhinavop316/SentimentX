{
  "name": "sentimentx-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend run start",
    "lint": "npm --prefix backend run lint",
    "test": "npm --prefix backend run test",
    "install:backend": "npm --prefix backend install"
  }
}
